name: Docker CI

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * 0"

jobs:
  build-linux:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Build for amd64
        run: |
          sudo apt update
          sudo apt install -y build-essential cmake curl git sudo llvm
          ./build.sh
          mkdir -p out
          mv /out/xeus-cling.$(uname -m).tar.gz out
      # - name: Build for arm64
      #   uses: uraimo/run-on-arch-action@v2.0.5

      #   with:
      #     arch: aarch64
      #     distro: ubuntu18.04
      #     githubToken: ${{ github.token }}
      #     dockerRunArgs: |
      #       --volume "${PWD}:/data"
      #     run: |
      #       apt update
      #       apt install -y build-essential cmake curl git sudo llvm
      #       /data/build.sh
      #       mkdir -p /data/out
      #       mv /out/xeus-cling.$(uname -m).tar.gz /data/out
      - name: Publish binaries to GitHub releases
        if: ${{ github.ref == 'refs/heads/master' }}
        uses: marvinpinto/action-automatic-releases@latest
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "latest"
          prerelease: false
          files: |
            out/*
